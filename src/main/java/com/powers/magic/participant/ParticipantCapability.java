package com.powers.magic.participant;

/** Capabilities that mechanics must request instead of assuming a living target is a player. */
public enum ParticipantCapability {
	PLAYER_POWER_STATE,
	ENERGY_POOL,
	CONSENT_OWNER,
	SUPPRESSION
}
