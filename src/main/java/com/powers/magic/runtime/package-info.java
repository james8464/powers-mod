/**
 * Owns bounded server-thread state for active magical fields, projectiles,
 * toggles, projections, and residues. Runtime records avoid client classes
 * and live entity references so cleanup remains deterministic.
 */
package com.powers.magic.runtime;
