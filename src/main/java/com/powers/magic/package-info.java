/**
 * Defines the immutable vocabulary, catalogue, scaling inputs, and pure
 * interaction rules shared by every POWERS cast origin. Classes in this
 * package contain no client state and are safe to load on a dedicated server.
 */
package com.powers.magic;
