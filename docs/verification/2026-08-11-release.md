# POWERS 1.0.2 finalisation verification — 2026-08-11

Target: Minecraft Java Edition 26.2, Fabric Loader 0.19.3, Fabric API 0.156.0+26.2, Java 25.

## Clean automated gate

The exact release tree passed:

```text
./test.sh check
BUILD SUCCESSFUL in 26s
```

- 587 JUnit tests passed with zero failures or errors.
- The clean check launched the real Fabric GameTest server; all 39 required live tests passed in 1.999 seconds.
- Common, client, test, and GameTest sources compiled with deprecation and unchecked warnings treated as errors.
- JSON, duplicate keys, PNG/Ogg decoding, models, translations, particles, sounds, recipes, loot, tags, advancements, dimensions, and deliberately absent crystal recipes passed strict validation.
- The 73-action catalogue and all 2,701 interaction pairs exactly match generated documentation.
- Exact manifests cover 553 production/client Java files and 160 non-item assets.
- The deterministic synthetic workload exercised 10, 50, and 100 players for 1,200 ticks each across real spatial indexes and rotating queues while enforcing 512 particles, 2,048 entity inspections, one packet per player/lane window, exact cleanup, and one body-proxy chunk ticket.

## Live runtime proof

The final build repeated all 39 required live tests in 1.916 seconds. Critical observed paths include Shadow Sword lightning entity creation, shared sacrificial forcefields, minimum-rank terrain effects, test-actor name/energy targeting, controlled mob possession and Dreamwalking, testing overrides, Miniportal recharge, crystal mindscape travel, living-force spread/containment/invasion, Celestial Ruin killing the First Vessel, and the historical Cinderheart block-impact crash path.

`./test.sh server` loaded POWERS 1.0.2 and reached `Done (0.203s)`. It accepted `/powers diagnose` and `/powers testing coverage`, reported no leaked runtime work in the empty world, then stopped cleanly. The save log enumerated `minecraft:overworld`, Nether, End, `powers:dark_realm`, `powers:light_realm`, and `powers:middleworld`.

`./test.sh client` loaded POWERS 1.0.2, the 23-power registry, both 28-node mazes, and the 73/2,701 collision kernel; initialized OpenGL 4.1 Metal on the Apple M3 Pro and OpenAL on the MacBook Pro speakers; and created the particle, block, item, celestial, and GUI atlases without a missing POWERS texture, model, mixin, or resource failure. Development-profile Mojang/Realms 401 messages are expected because Loom uses an offline identity and are not mod failures. The client was stopped after bootstrap and log inspection.

Subjective balance and catastrophic terrain acceptance remain staging-world checks even though the implementation and release gates are complete.

## Artifacts

- Runtime JAR: `build/libs/powers-1.0.2.jar`
- SHA-256: `792310cbfad07cbb524a2ec56575567f9d8034492ed78e80ee5f3d115aa52f50`
- Sources JAR: `build/libs/powers-1.0.2-sources.jar`
- Sources SHA-256: `235a955a5d3a28b19e22382c58a88d0972eac985b0a5549c02847e5c73f399f5`

The canonical requirement mapping is [`final-requirement-matrix.md`](final-requirement-matrix.md), and save changes are in [`2026-08-11-migration.md`](2026-08-11-migration.md).
