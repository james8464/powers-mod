# POWERS 1.0.2 finalisation verification — 2026-08-11

Target: Minecraft Java Edition 26.2, Fabric Loader 0.19.3, Fabric API 0.156.0+26.2, Java 25.

## Clean automated gate

The exact release tree passed:

```text
./gradlew clean test validatePowerResources auditJavaSources auditNonItemAssets verifyMagicDocs build --no-daemon
BUILD SUCCESSFUL in 26s
```

- 579 JUnit tests passed with zero failures, errors, or skips.
- The build task launched the real Fabric GameTest server; all 36 required live tests passed in 2.012 seconds.
- Common, client, test, and GameTest sources compiled with deprecation and unchecked warnings treated as errors.
- JSON, duplicate keys, PNG/Ogg decoding, models, translations, particles, sounds, recipes, loot, tags, advancements, dimensions, and deliberately absent crystal recipes passed strict validation.
- The 73-action catalogue and all 2,701 interaction pairs exactly match generated documentation.
- Exact manifests cover 552 production/client Java files and 160 non-item assets.
- The deterministic synthetic workload exercised 10, 50, and 100 players for 1,200 ticks each while enforcing 512 particles, 2,048 entity inspections, one packet per player/lane window, and one body-proxy chunk ticket.

## Live runtime proof

`./gradlew runGameTest --no-daemon` independently passed all 36 required tests in 2.121 seconds. Critical observed paths include Shadow Sword lightning entity creation, shared sacrificial forcefields, minimum-rank terrain effects, test-actor name/energy targeting, controlled mob possession and Dreamwalking, testing overrides, Miniportal recharge, living-force spread/containment/invasion, and Celestial Ruin killing the First Vessel's layered vitality.

`./gradlew runServer --no-daemon` reached `Done (0.230s)` and then stopped cleanly. The save log enumerated `minecraft:overworld`, Nether, End, `powers:dark_realm`, `powers:light_realm`, and `powers:middleworld`.

`./gradlew runClient --no-daemon` remained running after initialization. It loaded the 23-power registry, both 28-node mazes, and the 73/2,701 collision kernel; initialized OpenGL 4.1 Metal on the Apple M3 Pro and OpenAL on the MacBook Pro speakers; and created the particle, block, item, celestial, and GUI atlases without a missing POWERS texture, model, mixin, or resource failure. Development-profile Mojang/Realms 401 messages are expected because Loom uses the offline `Player611` identity and are not mod failures. The client was deliberately left open for the requested hands-on test session.

Subjective balance and catastrophic terrain acceptance remain staging-world checks even though the implementation and release gates are complete.

## Artifacts

- Runtime JAR: `build/libs/powers-1.0.0.jar`
- SHA-256: `04aacac0412a72e0ca9b9a6a177faa9601e1e301f058b76d7f8d2a3ec128d8b7`
- Sources JAR: `build/libs/powers-1.0.0-sources.jar`
- Sources SHA-256: `0d6fdfdf3446e4b0936cfeec546d3e1b9c8c04c0909d246a6baac7f12b2600be`

The canonical requirement mapping is [`final-requirement-matrix.md`](final-requirement-matrix.md), and save changes are in [`2026-08-11-migration.md`](2026-08-11-migration.md).
